@model IEnumerable<WebLocamer.Models.option>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.libelleOption)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prixOption)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libelleOption)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prixOption)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idOption }) |
            @Html.ActionLink("Details", "Details", new { id=item.idOption }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idOption })
        </td>
    </tr>
}

</table>
